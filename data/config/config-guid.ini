; config-gui - Basic template config file

; Loads regular config data
@config-gui.ini@

; Overrides some values for debug
[Display]
Title           = config-gui (Debug)

[Render]
ShowFPS         = true

[Resource]
WatchList       = Config # Texture # Sound
