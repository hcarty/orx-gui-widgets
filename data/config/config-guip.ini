; config-gui - Basic template config file

; Loads regular config data
@config-gui.ini@

; Overrides some values for profile
[Display]
Title           = config-gui (Profile)

[Render]
ShowFPS         = true
